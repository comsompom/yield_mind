# YieldMind — Initia Hackathon
# Flask app + API
flask>=3.0.0
python-dotenv>=1.0.0

# HTTP client for RPC / APIs
httpx>=0.27.0

# AI (OpenAI-compatible API or local)
openai>=1.12.0

# Optional: local dev / lint
ruff>=0.4.0
gunicorn>=22.0.0
pytest>=8.2.0
